version: '3.8'

services:
  fallback-backend:
    image: python:3.11-slim
    volumes:
      - ./app.py:/app.py
    ports:
      - "4444:4444"
    command: sh -c "pip install flask && python /app.py"
