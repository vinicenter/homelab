version: '3.8'
services:
  fallback:
    image: node:20-slim
    container_name: fallback
    restart: unless-stopped
    volumes:
      - ./fallback.js:/fallback.js
    command: node /fallback.js
    ports:
      - "4444:4444"
